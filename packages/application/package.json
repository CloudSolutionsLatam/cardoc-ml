{
  "name": "@cardoc/application",
  "version": "0.0.0",
  "private": true,
  "description": "Use-cases que orquestan dominio + puertos (providers, persistence). Sin Express ni SDK de Catalyst — testeable con fakes.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@cardoc/domain": "workspace:*",
    "@cardoc/persistence": "workspace:*",
    "@cardoc/providers": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "5.4.5",
    "vitest": "^1.6.0"
  }
}
