{
  "name": "@cardoc/persistence",
  "version": "0.0.0",
  "private": true,
  "description": "Tipos de fila + puertos de repositorio del Catalyst DataStore. Puro: el wiring concreto al DataStore lo inyecta la función.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "echo \"(sin tests aún — integración de repos en Fase 1)\" && exit 0"
  },
  "dependencies": {
    "@cardoc/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "5.4.5"
  }
}
